node_modules
.svelte-kit
.wrangler
.env
.dev.vars
dist
*.log
.DS_Store

# ⚠️ CF Workers Builds의 bun(1.2.15)이 bun 1.4의 lockfileVersion 2를 못 읽는다.
# 저장소엔 npm 락파일만 두고(빌드는 npm ci), 로컬은 bun을 그대로 쓴다.
bun.lock
