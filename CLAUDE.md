# AI SLOP

**AI로 만든 것을 모아 놓고 그게 슬롭인지 쓸모 있는 것인지 유저가 판단하는 커뮤니티.**
슬롭만 모으는 곳이 아니다 — 잘 쓴 사례가 같이 올라와야 판정이 믿을 만해진다.
홈은 GeekNews형 링크 피드, 상세는 Community Notes형 사건 페이지.
톤은 인터넷 렉카가 아니라 소비자 고발 미디어. 흐름은 `발견 → 제보 → 검증 → 여론 → 기록`.

## 왜 판별 툴이 아니라 커뮤니티인가

경쟁 상대는 AI 탐지기다. 네 곳에서 이긴다 —
① 탐지기는 확률만 뱉고 근거를 못 준다("87% AI"는 "그 툴 자주 틀리던데"로 무력화된다).
여기는 링크·캡처·판매 페이지가 근거로 남아서 반박하려면 근거를 반박해야 한다.
② **탐지기는 모델이 좋아질수록 나빠지고 여기는 좋아진다.** 결과물 한 장의 신호는
수명이 끝나가지만 사람이 보는 주변 증거(계정 이력·물량·가격·사업자 정보)는 안 낡는다.
③ 탐지기는 "AI냐"에서 멈춘다. 소비자가 알고 싶은 건 "그래서 이거 괜찮냐"고,
그건 축 2라서 탐지기가 원리적으로 못 간다.
④ 탐지기는 매번 0에서 시작한다. 여기는 같은 업체의 전적이 쌓인다 — **누적이 해자다.**

설치·환경변수·명령은 README, 스키마는 `src/lib/core/db/schema.ts`(상단 주석에 "일부러 안 만든 것" 목록).

## 절대 어기지 않는 것

- ⚠️ **운영자는 슬롭인지 아닌지를 정하지 않는다**(2026-09-09 유저 지시 — "이건 유저들의
  공간이라니까? 왜 자꾸 운영자냐"). `post.verdict`·`verdictNote`·`EDITOR_ONLY_VERDICTS`·
  `ForbiddenVerdictError`를 전부 걷어냈다(`drizzle/0006`). **되살리지 마라** — 어떤 이름으로도
  (판정·등급·검증·에디터 픽) 다시 만들지 마라. 품질은 `vote` 테이블과 `opinion()`이 답한다.
  ⚠️ AI 생성 여부(`aiStatus`·`aiEvidence`)도 같은 지시로 걷어냈다(`drizzle/0008`).
  운영자가 확정하는 **사실 필드가 하나도 없다** — 남은 판단 장치는 `vote`뿐이다.
- **운영자가 하는 일은 사실 확인뿐이다.** 분류, 아카이브 사본, 요약 다듬기,
  신고·정정·당사자 답변 처리. 그게 `reviewPost`의 전부다.
- **투표는 두 방향뿐이다**(슬롭이다 / 괜찮다). 한때 "추천은 하나뿐"이었고 종류별 반응을
  금지했는데, 그건 종류가 정렬에서도 판정에서도 **아무 일을 안 했기** 때문이다.
  그 조건("그 종류가 무슨 일을 하는가")에 답이 생겨서 열었다 — **표가 여론을 만든다.**
  ⚠️ 여기서 더 쪼개지 마라(기만적·돈값 못함·불쾌함…). 이유를 남길 자리는 댓글이고
  그게 근거로도 낫다. 정렬(`heat`)은 방향 무관 총 투표수라 논쟁 중인 게 위로 온다.
  ⚠️ 투표는 로그인 필수다 — 표가 여론이 되는 순간 판정당할 업체가 직접 몰려온다.
- ⚠️ **표가 모자란 글이 다수인 게 정상 상태다.** 스코프가 "AI로 만든 모든 것"이라
  물량이 많다. 여론이 없는 걸 비정상으로 보고 숨기면 피드가 텅 빈 것처럼 보인다.
- **점수를 만들지 마라.** 표본이 적을 때 숫자는 정밀한 척만 하고 공격 소재가 된다.
  0~10점 6축 평가는 2026-09-08에 테이블째 걷어냈다. 사례가 쌓인 뒤에 다시 논의한다.
  ⚠️ 여론 비율도 같은 이유로 표본 임계(`VOTE_MIN`) 아래면 숫자를 숨긴다 — 3표에 100%는
  정밀한 척만 하고 그대로 공격 소재가 된다.
- **AI라는 이유만으로 슬롭이 아니다.** 스코프는 "AI로 만든 모든 것"이고 잘 쓴 사례도
  받는다. ⚠️ 접수 단계에서 "이건 슬롭이 아닌 것 같다"로 거르지 마라 — 뭐가 슬롭인지
  고르는 건 유저 몫이고, 전부 유죄인 기관은 아무도 안 믿는다.
- ⚠️ **글쓰기 폼은 제목·본문·캡처 셋이다**(2026-09-09 유저 지시 — "허들이 너무 많다").
  칸을 다시 늘리지 마라. URL·요약·유형·AI 근거·자료 URL·직접경험 체크를 걷어냈고,
  URL은 본문 첫 링크, 요약은 본문 발췌(`core/text.ts`), 유형은 운영자 검토가 정한다
  (그래서 분류에 `etc`가 있다). **운영자만 아는 값을 제보자에게 묻지 마라.**
  관계자 체크만 남았다 — 이해충돌 고지라 지우면 안 된다.
- ⚠️ **본문은 플레인 텍스트다. 리치텍스트도 마크다운도 넣지 마라**(2026-09-09 결정).
  리치텍스트는 HTML을 저장하게 되어 새니타이저가 필요해지고, 판정 대상이 AI가 만든
  페이지라 복붙하면 저쪽 마크업이 통째로 들어온다. 둘 다 허용하면 렌더러도 두 벌이다.
  자동 링크는 `core/text.ts`가 조각으로 쪼개고 `Body.svelte`가 그린다 — `{@html}` 금지.
  플레인 → 마크다운은 나중에 올라갈 수 있고 리치텍스트에서는 못 내려온다.
- ⚠️ **글은 바로 게시된다**(2026-09-09 유저 지시 — "긱뉴스처럼"). 사전 검토 게이트를
  되살리지 마라. 그 자리를 메우는 건 사후 창구 셋이다 — **글 신고**(`post_report`,
  비로그인) · 정정 요청 · 당사자 답변. 하나라도 없애면 실명 판정 사이트에 방어선이 없다.
  운영자 대응은 `hidePost`(soft delete + 감사 로그) 하나다.
- **삭제 대신 soft delete + 감사 로그.** `audit_log`에 DELETE 경로를 만들지 마라 —
  D1엔 권한으로 막을 방법이 없어 "코드에 그 경로가 없다"가 유일한 강제다.
- **돈 받고 지우는 기능 금지.** 결제·삭제요청을 잇는 어떤 배선도 만들지 마라.
- **당사자 답변·정정 요청은 비로그인 제출.** 가입을 요구하면 반론 창구가 닫힌다.
  정정은 수용·반려 **둘 다** `/about#정정`에 공개한다(`/corrections`는 2026-09-08에 합쳤다).
  ⚠️ 둘은 상세가 아니라 **`/posts/[slug]/reply`**에 있다(2026-09-09 유저 지시). 상세 밑에
  접어 두면 판정 글을 읽으러 온 사람에겐 잡음이고 정작 쓸 업체는 끝까지 안 내려온다.
  상세에 남은 건 `신고` 버튼과 그 페이지로 가는 버튼 둘뿐이다.
- ⚠️ **시드에 실제 판정을 넣지 마라.** 저장소는 공개돼 있고 코드만 담는다 —
  판정 데이터가 시드로 들어가면 사이트가 통째로 복제 가능해진다.
- **데모 데이터를 만들지 마라.** 시드 스크립트는 2026-09-08에 삭제했다(유저 지시).
  화면의 DEMO 표시(`DemoBadge.svelte`)도 지웠으므로 가짜가 들어오면 진짜와 구분할 방법이 없다.
  화면을 채워 보고 싶으면 `/submit`으로 직접 넣고 `/admin`에서 게시해라 —
  그게 실제 흐름을 같이 검증한다. 되살릴 일이 생기면 git 히스토리에 있다.

## 디자인 — Lobste.rs 이식 (2026-09-08 유저 선택)

**핵심은 AI 냄새를 빼는 것.** 그림자·그라데이션·트랜지션·콜아웃(좌측 액센트 바 + 틴트
배경)·이모지·웹폰트는 지금 AI가 뱉는 화면의 문법이라 전부 금지.

⚠️ **팔레트를 지어내지 마라.** `app.css`의 값은 lobste.rs `light-normal` 테마에서
그대로 뽑은 것이다(`--base-fg: 51 51 51`, `--color-fg-link: rgb(28 89 209)`, 태그 3종 등).
바꿔야 하면 레퍼런스에서 다시 뽑아라.

- 구조(전부 원본 실측값): wrapper `max-width: 76rem`(= PH 실측 1216px) · 산문 `.prose`는 `45rem` ·
  좌측 40px 투표 칼럼 · 제목 + (도메인) + 칩 한 줄 · byline 한 줄.
  **목록은 넓게 훑고 본문은 좁게 읽는다** — 컨테이너 폭과 산문 폭을 같게 만들지 마라.
- ⚠️ **byline 링크는 회색**(`.meta a { color: inherit }`). 아이디·댓글·도메인까지 파래지면
  한 행에 파란 덩어리가 넷이 되어 제목이 안 보인다. 원본도 회색이다.
- ⚠️ **색이 있는 자리는 제호 하나뿐이다**(2026-09-08 유저 지시 — "프러덕트헌트는
  포인트색이 거이없네. 나도 포인트색없이 로고만있게가자"). PH를 실측하니 정말로
  로고 말고는 전부 무채색이다(`#101828` 본문 · `#344054` 보조 · `#98A2B3` 흐림).
  **칩·링크·화살표·형광펜에 색을 넣지 마라.** 구분은 색이 아니라 **명도와 무게**로 한다:
  AI 여부 = 테두리만, 문제 있음 = 검정 채움, 괜찮음 = 테두리 진하게, 눌린 표 = 진하게.
  ⚠️ 예외는 둘로 늘었다(2026-09-09 유저 지시 — "리스트에 뱃지처럼 갈색으로") —
  목록 여론 네모(`.opinion`)와 상세 "슬롭이다" 버튼(`.btn-slop`). 같은
  `--color-negative` 하나를 써서 목록에서 걸린 갈색이 상세 버튼으로 이어진다.
  **"괜찮다"에는 칠하지 마라** — 둘 다 칠하면 신호등이 되고 색이 어느 쪽을 누르라고 민다.
- ⚠️ **링크도 무채색이다**(`--color-link: #101828`). lobste.rs 원본 파랑(#1c59d1)에서
  뺐다 — 칩에서 색을 없애고 나니 목록의 파란 제목이 화면에서 유일하게 튀는 색이 됐다.
  산문 안 링크는 `.prose a`에서 **밑줄**로 알린다. 파랑으로 되돌리지 마라.
- **종이는 흰색**(2026-09-08 유저 지시 — "쓰레기 사이트가 되어야 한다. 근데 배경색은
  흰색이다. 랍스터처럼"). 배경을 누런 갱지로 칠하는 건 이미 해보고 되돌렸다 —
  판정 기관이 똥색이면 판정을 못 믿는다. 판정색을 원색 빨강·초록으로 만드는 것도
  거쳐 왔다(렉카 배지처럼 보인다) → 똥색·이끼색 → 지금은 아예 무채색이다.
- ⚠️ **색을 지어내지 마라**(유저 지시). 출처는 셋뿐이다 — lobste.rs에서 뽑은 값,
  **Tailwind 기본 팔레트**, PH 실측값. `app.css`에 출처를 적어 뒀다.
- 브랜드색은 **똥색**(`--color-sludge: #854d0e` = Tailwind yellow-800)이고 제호(`.logo`)·favicon에만.
  ⚠️ 산성 녹색(#c8f000) → 겨자색 → #5f4413(**돌색**) → #8a5a0f(너무 밝음) → 지금 값.
  지어내지 말고 Tailwind yellow/amber 800~900에서 골라라.
- ⚠️ **제호에 배경을 깔지 마라**(유저 지시). 색 블록으로 칠하면 판정 칩과 같은
  덩어리로 보여 브랜드와 판정이 섞인다. 제호는 글자, 판정은 칩 — 형태로 구분한다.
- 재사용 클래스: `.score .tag .tag-media .tag-meta .tag-negative .tag-affirmative .box .bar
  .btn .btn-primary .meta .num .mark`
- AI 냄새 차단 블록(`app.css` 맨 아래: no transition/animation/box-shadow)을 지우지 마라 —
  지우면 디자인이 조용히 원래대로 돌아간다.
- **제호 마크는 똥 실루엣**(2026-09-08 유저 지시, `ais` 모노그램 폐기). 같은 도형이
  `static/favicon.svg`와 `+layout.svelte`에 각각 인라인으로 있다 — 한쪽만 고치지 마라.
  ⚠️ 이모지(💩)로 대체하지 마라: 폰트마다 다르게 그려지고 이 사이트는 이모지를 안 쓴다.
- **제호는 도메인 그대로 `aislop.kr`**(FactCheck.org 방식). 모노그램 `ais`에서 되돌렸다 —
  이름과 카테고리가 같은 말이라 이름만으로는 구별이 안 되고, 하나뿐인 건 도메인이다.
  제호가 곧 주소면 스크린샷으로 퍼져도 출처가 같이 간다. `.kr`은 브랜드색 밖에 둔다.
- **거쳐 온 것**(다시 제안하지 마라): GeekNews 모방 → "너무 똑같다", 2000년대 한국
  게시판 → "구시대 UI도 보기 힘들다", 배경 전체 똥색 → 판정 신뢰가 깨진다.
  헤더 한글 병기·태그라인도 유저가 뺐다.
- ⚠️ **타이포는 PH 실측 스케일**(2026-09-08 유저 지시 — "폰트들도 좀 프러덕트헌트처럼
  큼지막하게"). PH 값은 `24/600` 제목 · `16/600` 제품명 · `16/400` 본문 · `14` 메타,
  줄높이 24px다. 한글은 라틴보다 획이 많아 한 단 올려 쓴다:
  **페이지 제목 24px · 목록 행 제목 20px · 소제목 18px · 본문 16px · 메타·칩 14px.**
  ⚠️ **바닥은 14px다.** lobste.rs 원본(.meta 0.8rem, .tag 0.66rem, .btn 0.82rem)으로
  되돌리지 마라 — 그 값은 라틴 기준이고 한글은 같은 크기에서 훨씬 흐려진다.
- **홈에서 3초 안에 읽혀야 할 세 가지**: 무슨 사례인지 / AI가 확인됐는지 / 왜 문제인지.
- **헤더 탭은 랍스타 구조**(2026-09-08 유저 지시): 활발 / 최신 / 댓글 / 검색.
  여론별 목록(슬롭·애매·괜찮음)과 `AI 확인`은 탭이 아니라 **필터 줄**(`OPINION_FILTERS`)이다.
  ⚠️ 필터를 운영자 축으로 되돌리지 마라 — 한때 검증됨·논쟁 중·Not Slop이었는데
  운영자 판정이 없어져서 목록이 통째로 비었다. 라벨은 한글이다(영문은 안 쓴다).
- ⚠️ **`.meta a`에 `:not(.btn)`을 유지해라.** 안 그러면 `.meta`(0,1,1) 안의
  `.btn-primary`(0,1,0) 흰 글씨가 회색으로 덮여 제보 버튼이 안 보인다.
- ⚠️ **목록의 칩은 하나.** 여론이 있으면 여론, 없으면 없다. AI 상태는 byline 평문이다.
  상세(`full`)에서는 AI 여부 + 여론 둘을 보여준다.
- ⚠️ **칩에는 색이 없다**(위 색 규칙 참조). 채움이 있는 건 "문제 있음" 하나고 검정이다 —
  한 행에 채운 칩이 둘이면 어느 쪽이 결론인지 안 읽힌다.
  정보량이 없는 값(미판정·AI 불명·AI 정황)은 목록에서 **아예 뺀다**(`AI_STATUS_IN_FEED`) —
  "모르겠다"는 칩은 정보량이 0인데 자리는 똑같이 차지해 목록을 잡음으로 채운다.
  상세(`full`)에서는 원칙대로 두 축을 전부 보여준다.
- ⚠️ **피드 한 행 = 두 줄, 칩 하나** (2026-09-08 유저 지적 — "글이 너무 많아 피로").
  요약문을 피드에 되살리지 마라(상세에 있다). AI 상태·분류는 byline 평문이다.
  **매 행에 같은 칩이 반복되면 정보가 아니라 잡음이다.**
  컨테이너를 넓히는 건 해법이 아니다 — 800px/16px가 이미 한 줄 75자로 적정 길이고,
  넓히면 줄만 길어져 더 피곤해진다. 밀도는 폭이 아니라 행당 정보량 문제다.
- **제목은 우리 기록으로, 도메인은 원문으로** (2026-09-08 확정). Lobste.rs/HN처럼
  제목을 원문에 걸지 마라 — 감별 미디어가 감별 대상에게 트래픽과 광고 수익을 보내는
  구조가 된다. 원문은 괄호 안 도메인에 `rel="nofollow ugc noopener"`로만 건다.
  덧붙여 슬롭은 지적당하면 잘 내려가고(그래서 `archiveUrl`이 있다) 목격담은 원문이
  아예 없기도 하다 — 제목이 원문으로 가면 깨진 링크가 대문이 된다.
- **상세 상단은 피드 행과 같은 줄이다**(lobste.rs 댓글 페이지 방식). 목록에서 넘어온
  사람이 같은 줄을 다시 보고 자기가 뭘 눌렀는지 확인한다.
- **상세는 평범한 글이다** (유저 지적 — "너무 체계있어서 피곤"). 네 질문(무엇이 게시됐나 /
  AI 확인됐나 / 왜 문제인가 / 왜 Slop인가)은 **스키마에만** 남기고 화면에서 소제목으로
  칸을 나누지 마라. 시각적으로 떼는 블록은 **당사자 답변**뿐이다.

## 문장 — AI 말투 배제

⚠️ 취향이 아니라 **기능**이다. AI 슬롭을 판정하는 사이트가 AI가 쓴 것처럼 읽히면
그 자체가 반박 재료가 된다. 디자인의 AI 냄새 차단 블록과 같은 급의 규칙이다.

- **문체는 자리로 나눈다.** 셋이다 — 산문은 **평서형 `~다`**, 표·목록·힌트는 **명사형**,
  UI 부스러기는 **음슴체**.
  ⚠️ `~습니다`로 되돌리지 마라(2026-09-09 유저 지시 — "니다 반복이 ㅈㄴ 불쾌하다.
  AI로 만든 사이트나 광고도 다 이런 식"). 한국어 평서체는 종결어미가 사실상 하나라
  문장마다 끊어 쓰면 같은 북소리가 나고, 그게 AI가 쓴 글의 대표적인 냄새다.
  `~다`는 반말이 아니라 **신문·백서 문체**라 고발 미디어에 오히려 맞고, 언론이 인용할
  때도 같은 문체다. 해체(`~해`·`~야`)로 내려가는 건 다른 얘기다 — 그건 하지 마라.
  - 산문 평서형: `/about`·`/ai-slop` 본문, 원칙 선언, 정정 정책, 푸터 원칙 한 줄
  - 명사형: `taxonomy.ts`의 힌트 전부, `dl` 목록의 `dd`, 분류 설명
  - 음슴체: 캡션·라벨·버튼 주변·상태 표시·토스트·에러·빈 화면 (`접수함`, `댓글 없음`)
    ⚠️ **음슴체는 `~함/~음`이나 명사형 종결이지 `~해라`가 아니다**(2026-09-08 유저 지적 —
    "음슴체인데 너무 예의없게는 하지 마라"). 짧게 쓰려다 명령형으로 미끄러지기 쉽다:
    `신고 사유를 적어라`(X) → `신고 사유를 입력`(O), `너무 잦음. 잠시 후 다시`(X) →
    `요청이 잦음. 잠시 후 다시 시도`(O). 기준선은 `login/+page.server.ts`다.
  - ⚠️ **에러 문체를 두 갈래로 만들지 마라.** 한때 login만 음슴체고 `validate.ts`·
    `guard.ts`·admin은 "~해 주세요"였다(2026-09-08 통일). 새 에러는 음슴체로 적어라.
  ⚠️ 판정당한 쪽이 캡처해서 인용하는 글은 진지해야 한다. 정정 페이지는 기업 법무가 읽는다.
- ⚠️ **나열을 셋으로 맞추지 마라**(2026-09-09). `A하고, B하고, C합니다`가 세 번 겹치면
  그 리듬 자체가 AI의 서명이다. `/ai-slop`·`/about`에 정확히 세 항목짜리 나열이 여섯
  군데 있었다. 사람은 둘로 끝내거나 넷까지 늘어놓거나 하나만 던진다. **개수를 흩뜨려라.**
- **문장 길이를 들쭉날쭉하게.** 조각 문장(`슬롭만이 아니라 잘 쓴 것도.`)을 허용한다.
  같은 길이 문장이 다섯 개 이어지면 내용과 무관하게 AI 티가 난다.
- **굵게는 문단당 하나.** 서넛을 뿌리면 아무것도 안 걸린다.
- **문단을 결론으로 닫지 마라.** `그래서 …입니다`로 매 문단을 마무리하는 게 AI 버릇이다.
- **청유·부탁형 금지.** `적어 주십시오` → `적으면 된다`. 무료 사이트가 굽신거릴 이유가 없다.
- ⚠️ **문장 끝을 em dash로 받지 마라.** `…입니다 — 왜냐하면` 형태를 산문에 쓰지 마라.
  문장을 끊고 다음 문장으로 이어라. (이 CLAUDE.md 안에서는 써도 된다. 개발자용이다.)
- **반복 템플릿 금지.** 여러 화면이 `**굵게** — 설명.` 같은 같은 모양으로 늘어서면
  그 규칙성이 곧 AI 티다. 탭 캡션 넷이 그랬고 2026-09-08에 걷어냈다.
  같은 날 목록 넷에 또 `~한 사례.` 캡션이 생겨서 다시 걷어냈다 —
  목록 이름이 곧 설명이라 정보량이 0이었다.
  두 축 혼동을 막는 `/verified` 한 줄만 남겼다. **나머지에 캡션을 다시 달지 마라.**
- **따옴표 뜻풀이 금지.** `"이 기록이 볼 만하다"는 뜻이고` 같은 문장을 쓰지 마라.
  뜻을 설명해야 한다면 문장이 잘못된 것이다. (그 문구가 투표 버튼 `title`에 그대로
  살아 있던 걸 2026-09-08에 뺐다 — 규칙을 적어 놓고 정작 코드에 남아 있었다.)
- **피동·명사화를 능동으로.** 접수되었습니다 → 접수했습니다. 게시됩니다 → 게시합니다.
- **라벨은 명사, 버튼은 동사.** `어떤 유형인가요?` → `유형`. 물음표 라벨을 쓰지 마라.
- **사과·격려·감탄 금지.** 빈 화면에 "첫 제보를 기다립니다" 류를 넣지 마라(유저 지시).
  에러는 무엇이 틀렸고 뭘 하면 되는지만 적는다.
- **이모지·느낌표 없음.** `app.css` 규칙과 짝이다.
- **같은 행동은 같은 단어로.** 사례를 올리는 행동은 **글쓰기**다(2026-09-09 유저 지시).
  스코프가 "AI로 만든 것 전부"로 넓어져서 `제보`는 고발만 받는 것처럼 읽힌다.
  헤더 버튼·`/submit` 제목·완료 화면이 전부 그 말이다. `제보하기`로 되돌리지 마라.
- 판정 문구는 **단정형 대신 관찰 + 근거**로. 명예훼손 방어선(형법 310조)이 여기에 걸려 있다.

## 구조 결정

- 닫힌 도메인의 진실원은 `core/taxonomy.ts`의 TS union. DB엔 CHECK로만 복제하고
  룩업 테이블을 만들지 마라. 화면 라벨도 여기서만 고친다.
- **DB에 닿는 모듈에서 화면용 상수를 export하지 마라.** `FEED_TABS`가 `feed.ts`에 있었을 때
  레이아웃이 그걸 값으로 import해 `node:async_hooks`가 클라이언트 번들로 끌려갔고,
  SSR은 200인데 하이드레이션만 죽었다(curl로는 안 잡힌다).
- **분류 숨기기(`/filters`) 화면을 되살리지 마라**(2026-09-08 유저 지시 — "헤더에 이게
  다 있네"). `CategoryBar`가 이미 분류·판정을 전부 링크로 깔고 있어서, 별도 페이지는
  쿠키·서버액션·라우트를 하나씩 더 지고 겹치는 일을 했다. 사례가 쌓여 목록이 길어지면
  그때 다시 논의한다.
- 시각은 ISO8601 KST 문자열(`core/time.ts`). 예외 없다.
- 권한 검사는 서버에서만. `locals.user`는 `hooks.server.ts`가 유일하게 채운다.
- 평판(제보 수·게시 수 등)은 계산값이다. 캐시 테이블을 만들지 마라. 배지는 손으로 붙인다 —
  자동 부여를 만들면 제보 수 늘리기 게임이 시작된다.

## 함정

- **락파일이 둘이다.** 로컬은 bun(`bun.lock`, gitignore됨), CI는 npm(`package-lock.json`, 커밋됨).
  ⚠️ CF Workers Builds의 bun이 1.2.15라 bun 1.4가 쓰는 lockfileVersion 2를 못 읽는다
  (`bun install --frozen-lockfile`이 여기서 죽는다). 의존성을 바꾸면 **둘 다** 갱신해라:
  `bun install && npm install --package-lock-only`.
  빌드 환경 bun이 올라가면 `package-lock.json`을 지우고 `bun.lock`을 다시 커밋하는 게 맞다.

- **`.kr` 도메인**: Cloudflare 레지스트라가 `.kr`을 취급하지 않는다. 국내 등록기관에서
  등록하고 **네임서버만 Cloudflare로 위임**한 상태여야 Worker custom domain이 붙는다.

- **한글 slug + Location 헤더**: non-ASCII를 못 싣는다. `redirect()`에 slug를 넣을 땐
  반드시 `encodeURIComponent`. `params.slug`는 이미 디코딩된 값이다.
- **zod `z.url()`은 스킴을 안 본다** — `javascript:alert(1)`이 통과해 `<a href>` XSS가 된다.
  `validate.ts`의 `httpUrl()` / `core/url.ts`의 `safeHttpUrl()`을 써라. 관리자 입력도 포함.
- **로그인 = 이메일 OTP 하나뿐**(2026-09-08). 비밀번호도 소셜 OAuth도 없다. 되살리지 마라 —
  콘솔 등록 3곳·시크릿 4개가 배포를 막고 있었고, 카카오는 이메일이 잠긴 항목이라
  자리표시자 주소를 심어야 했다.
- **인증 라이브러리를 쓰지 않는다**(better-auth 제거, 2026-09-08 유저 지시). 전부
  `core/auth.ts` 한 파일이고 의존성이 없다. 규칙 넷은 `auth.test.ts`가 묶고 있다:
  `crypto.getRandomValues`만 · 코드/토큰은 해시로만 저장 · 상수 시간 비교 · 5분 3회.
  ⚠️ 라이브러리를 다시 넣지 마라 — 쓰던 기능이 5%였는데 맞추는 코드가 그만큼 있었다.
- OTP는 이름을 안 준다 → `ensureName`이 `slop-xxxx`를 발급한다.
  ⚠️ **회원 식별자는 이메일과 닉네임(`name`) 둘뿐이다**(2026-09-09 유저 지시 —
  "걍 이메일, 닉네임 두 개로 가면 안 되냐"). 표시 이름과 URL 키를 나눠 두던
  `username`을 걷어냈다(`drizzle/0010`) — `renameUser`가 늘 name을 slugify해 채우던,
  값이 하나뿐인 두 칸이었다. `name`은 UNIQUE(사칭 차단)고 프로필 주소는 `/users/<id>`라
  이름을 바꿔도 링크가 안 죽는다. 옛 주소 리다이렉트 표를 만들지 마라 — "예전 이름"이
  영구 기록으로 남아 이름을 바꾼 이유를 되돌린다.
  ⚠️ 이메일 앞부분을 이름으로 쓰지 마라 — 작성자가 공개되므로 이메일이 공개된다.
- **dev는 늘 로그인 상태다**(`.dev.vars`의 `DEV_USER_ID`, hooks.server.ts:70).
  ⚠️ **화면을 고칠 땐 로그아웃 상태를 따로 봐라.** 이걸 안 해서 비로그인 방문자에게
  기여 동선이 하나도 없는 걸 오래 못 봤다(2026-09-10 퍼널 점검). 보는 법:
  `DEV_USER_ID`를 주석 처리하고 다른 포트로 `bun run dev --port 3301`을 띄운다
  (부팅 뒤 되돌리면 된다 — 값은 시작할 때 한 번만 읽는다).
- **빈 dev DB에서는 판정 화면을 못 본다.** 표가 없으면 목록 좌측이 전부 "표 없음"이라
  슬롭·박빙·괜찮음이 화면에 안 나온다. `bun run db:seed:local`로 여섯 상태를 하나씩
  깐다(`seed.local.sql`). ⚠️ 그 파일의 여섯 줄 균형을 깨지 마라 — 이유는 파일 머리에.
- **drizzle 에러**: UNIQUE 위반 메시지는 `cause` 체인에 있다 — `isUniqueViolation()`을 써라.
- **CSP**: 외부 출처를 붙일 땐 `hooks.server.ts`부터 고쳐라. 안 고치면 조용히 차단된다.
  ⚠️ 썸네일 URL이 외부 도메인이면 `img-src`에 추가해야 뜬다(지금은 cdn.aislop.kr만 허용).

## 판정 문서

- ⚠️ **`/ai-slop`의 구별법은 유통기한이 있다.** "손가락 여섯 개·뭉개진 글자"를 신호로
  적어 뒀다가 2026-09-08에 걷어냈다 — 2023~24년 모델 흔적이라 지금은 잘 안 나온다.
  **판정 기관이 낡은 판별법을 가르치면 그 자체가 반박 재료가 된다.** 지금 세운 순서는
  계정 → 출처 → 여러 장 사이의 일관성 → 글 → 이미지 자체(보조)다. 결과물 한 장을
  뜯는 신호는 계속 약해지므로 **주변 신호를 위에 두는 순서를 뒤집지 마라.**
- ⚠️ **`/about` 첫 두 문단(피로) → `#왜`(공공의 이익) 순서를 뒤집지 마라**
  (2026-09-08 유저 지시). 사람을 부르는 건 "지겹다"는 공감이고 판정을 지탱하는 건
  공공의 이익이다. 감정을 판정 근거 자리로 올리면 "AI 싫어하는 애들"이 되어 판정이
  통째로 의심받고, 동기를 빼면 소비자원 안내문이 되어 아무도 안 모인다.
  `/about`에 "역겹다" 류를 쓰지 마라 — 판정당한 쪽이 캡처해 인용하고 기업 법무가
  읽는 문서다. 그 감정은 홈과 사례 본문에서 쓴다.
- ⚠️ **`/about#왜`(공공의 이익)와 `#운영`을 지우지 마라.** 실명 서비스를 판정하므로
  형법 310조(진실한 사실 + 오로지 공공의 이익 → 위법성 조각)가 방어선이고, 목적을
  사이트가 스스로 밝혀 두는 게 그 출발점이다. 원칙만 있고 목적이 없던 상태였다.
  운영 주체·연락처(ekankr2@gmail.com)·후원 없음·광고 없음도 같은 이유로 공개한다.
- ⚠️ **푸터에 `by eklab` 같은 자기 사업체 크레딧·링크를 달지 마라**(2026-09-08 결정).
  `#운영`의 "광고와 제휴 링크를 싣지 않습니다"와 정면으로 부딪히고, 판정당한 쪽이
  "웹 개발 영리 업체가 운영한다" 한 줄로 판정 전체를 홍보로 재프레이밍한다.
  eklab은 `#운영`에 **링크 없이 이해충돌 고지로만** 적혀 있다 — 먼저 밝힌 이해관계는
  공격 재료가 안 되지만 숨겼다가 발각되면 은폐가 된다. 홍보가 필요하면 방향을
  뒤집어라: eklab 쪽에서 aislop을 제작 사례로 소개한다(백링크 방향도 그쪽이 맞다).

## 상태

- MVP 동작: 피드(탭 4 + 여론·분류 필터) · 상세 · 글쓰기(즉시 게시) · 이메일 OTP 로그인 ·
  투표(여론) · 댓글 · 글·댓글 신고 · 관리자 사실 확인 · 당사자 답변 · 정정 · 검색 ·
  프로필 · sitemap/RSS.
- **2026-09-08 배포 완료.** aislop.kr 실서비스, www→apex 301, 원격 D1 마이그레이션 적용,
  Email Service 발신 인증 완료. 시크릿은 없다.
- 남은 것: 사례 0건(진짜 병목), 사례별 OG 이미지(공용 1장).
- 메일: 나가는 건 셋뿐이다 — 로그인 코드 · 창구 접수 알림(`ADMIN_EMAIL`) ·
  처리 결과(보낸 사람에게, **반려도 보낸다**). 셋 다 상대가 먼저 행동해서 생기는 1:1
  메일이다. ⚠️ 구독·마케팅을 붙이지 마라 — 스팸 신고를 맞으면 로그인 메일까지 죽는다.
  알림은 `notify()`라 실패를 삼킨다(접수는 이미 끝났다). 로그인 코드는 그대로 던진다.
- 메일: SPF(`include:_spf.mx.cloudflare.net`)·DMARC(`p=reject`)는 들어갔다.
  ⚠️ 첫 발송이 Gmail 스팸함으로 갔던 뒤로 **다시 안 재봤다.** 사람을 부르기 전에
  로그인 코드 한 통을 지메일로 보내 받은편지함에 들어가는지부터 확인해라 —
  스팸으로 가면 가입이 통째로 막히고, 그건 사례 0건보다 먼저 걸리는 병목이다.
- 세션 30일 고정(슬라이딩 아님, 2026-09-08 유저 결정). 코드 5분·3회.

## SEO

**"AI 슬롭"과 "AI slop"은 한국에서 아무도 안 먹은 빈 키워드다**(2026-09-08 확인).
다만 빈 건 경쟁이지 수요가 아니다 — 검색량이 0이라 그 키워드만으로는 유입이 안 온다.

- **이름 = 브랜드용, 유입 = 다른 키워드.** 실제 질의는 "AI 그림 구별법", "챗지피티 글 티",
  "AI 소설 전자책", "가짜 후기 구별" 쪽이다. 그 질의에 답하는 글이 유입을 만들고,
  거기서 "AI 슬롭"을 반복 노출해 브랜드 키워드를 나중에 키운다. 순서를 뒤집지 마라.
- 제목 접미사는 `AI 슬롭(AI slop)` — 두 표기를 전 페이지에서 같이 노출한다(`Seo.svelte`).
  ⚠️ 가운뎃점으로 되돌리지 마라 — 홈에서 `이름 · 이름 — 설명`으로 구분자가 겹치고
  두 표기가 별개 이름처럼 읽힌다. 괄호는 신문·사전 방식이라 **한 이름**으로 읽힌다.
- ⚠️ **홈 title은 키워드를 노리지 않는다**(2026-09-08 유저 결정). "AI 슬롭 뜻"류는
  `/ai-slop`이 먹는 질의다 — 홈이 같이 밀면 구글이 둘 중 뭘 올릴지 헷갈려 서로 깎는다.
  홈 title의 일은 **공유됐을 때 뭐 하는 곳인지 한 줄로 알리는 것** 하나다:
  `AI 슬롭(AI slop) — AI로 만든 것을 모아 슬롭인지 아닌지 같이 판단한다`.
  이 문장이 하는 일 셋: 스코프(AI로 만든 것 **전부**) · 커뮤니티("같이") ·
  판단 주체가 유저("판단합니다"). 셋 중 하나도 빼지 마라.
  거쳐 온 것 — "AI로 찍어낸 것을 근거로 판정합니다"(정체는 읽혔는데 커뮤니티 신호가 0),
  "…지친 사람들이 사례를 모읍니다"(사람은 모이는데 **슬롭만 모으는 곳**으로 읽혀
  스코프를 좁혔다).
  ⚠️ "기록합니다"(위키 소리) · "지겨운 AI"(AI 전체를 싫어하는 말로 읽혀 사이트
  전제와 부딪힘)로 바꾸지 마라. 같은 문장을 rss.xml·auth.ts 메일 서명도 쓴다.
- 홈에만 `WebSite`+`Organization` JSON-LD를 붙인다(`siteJsonLd`). `alternateName`이
  두 표기를 한 엔티티로 묶는 자리다. SearchAction·logo는 일부러 뺐다(이유는 `jsonld.ts`).
- `/ai-slop` = 용어 정의 페이지. 이 사이트에서 **유일하게 SEO가 존재 이유인 문서**이고
  각인의 앵커다(언론이 인용할 때 걸리는 페이지). `/about`(원칙·정정)과 합치지 마라.
  `Article`+`DefinedTerm` JSON-LD를 붙였다(`aiSlopPageJsonLd`). 날짜는 손으로 관리한다 —
  문서를 실제로 고칠 때만 올려라.
- ⚠️ **`/about`에 AI 슬롭 정의를 다시 풀어 쓰지 마라.** 한때 `/ai-slop` 첫 문단을 그대로
  복사해 뒀는데, 두 페이지가 같은 예시 문장을 쓰면 구글이 유사문서로 보고 서로 깎는다
  (2026-09-08 분리). 정의·분류 설명은 `/ai-slop` 한 곳에서만 한다.
- 문서 페이지는 `<h1>`으로 시작한다(둘 다 `<h2>`였다). 절마다 `id`를 단다 —
  언론이 특정 절을 딥링크한다.
- ⚠️ **ClaimReview를 내보내지 마라.** 운영자가 확정하는 사실 필드가 하나도 없어서
  붙일 자리가 없다. 유저 표는 의견이고, 의견에 붙이면 구글 수동 조치 대상이다.
  고정은 `jsonld.test.ts`.
