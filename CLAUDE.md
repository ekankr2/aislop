# AI SLOP

**AI로 만든 것을 모아 놓고 그게 슬롭인지 쓸모 있는 것인지 유저가 판단하는 커뮤니티.**
잘 쓴 사례가 같이 올라와야 판정이 믿을 만해진다. 홈은 GeekNews형 링크 피드,
상세는 Community Notes형 사건 페이지. 톤은 인터넷 렉카가 아니라 소비자 고발 미디어.

**경쟁 상대는 AI 탐지기고, 이기는 지점은 넷이다.** 탐지기는 확률만 뱉고 근거를 못 준다 ·
모델이 좋아질수록 나빠진다(주변 증거는 안 낡는다) · "AI냐"에서 멈춰 "그래서 괜찮냐"에
원리적으로 못 간다 · 매번 0에서 시작한다. **누적이 해자다.**

설치·명령은 README, 스키마는 `src/lib/core/db/schema.ts`(상단 주석에 "일부러 안 만든 것").

## 절대 어기지 않는 것

- ⚠️ **운영자는 슬롭인지 아닌지를 정하지 않는다**(2026-09-09 유저 지시 — "이건 유저들의
  공간이라니까? 왜 자꾸 운영자냐"). `verdict` 계열(`drizzle/0006`)도 AI 생성 여부
  `aiStatus`·`aiEvidence`(`drizzle/0008`)도 걷어냈다. **어떤 이름으로도 되살리지 마라**
  (판정·등급·검증·에디터 픽). 운영자가 확정하는 사실 필드는 하나도 없고, 남은 판단
  장치는 `vote` + `opinion()`뿐이다.
- **운영자가 하는 일은 사실 확인뿐이다** — 분류, 아카이브 사본, 요약 다듬기, 신고·정정·
  당사자 답변 처리. `reviewPost`가 전부다.
- **투표는 두 방향뿐이다**(슬롭이다 / 괜찮다). ⚠️ 더 쪼개지 마라(기만적·돈값 못함…) —
  이유를 남길 자리는 댓글이고 그게 근거로도 낫다. 정렬(`heat`)은 방향 무관 총 투표수라
  논쟁 중인 게 위로 온다. ⚠️ 투표는 로그인 필수다 — 표가 여론이 되는 순간 판정당할
  업체가 직접 몰려온다.
- ⚠️ **표가 모자란 글이 다수인 게 정상 상태다.** 스코프가 "AI로 만든 모든 것"이라 물량이
  많다. 여론 없는 걸 숨기면 피드가 텅 빈 것처럼 보인다.
- **점수를 만들지 마라.** 표본이 적을 때 숫자는 정밀한 척만 하고 공격 소재가 된다.
  0~10점 6축 평가는 2026-09-08에 테이블째 걷어냈다. 여론 비율도 `VOTE_MIN` 아래면
  숫자를 숨긴다(3표에 100%).
- **AI라는 이유만으로 슬롭이 아니다.** ⚠️ 접수 단계에서 "이건 슬롭이 아닌 것 같다"로
  거르지 마라 — 고르는 건 유저 몫이고, 전부 유죄인 기관은 아무도 안 믿는다.
- ⚠️ **글쓰기 폼은 제목·본문·캡처 셋이다**(2026-09-09 유저 지시 — "허들이 너무 많다").
  URL·요약·유형·AI 근거·자료 URL·직접경험 체크를 걷어냈다. URL은 본문 첫 링크, 요약은
  본문 발췌(`core/text.ts`), 유형은 운영자가 정한다(그래서 분류에 `etc`가 있다).
  **운영자만 아는 값을 제보자에게 묻지 마라.** 관계자 체크는 이해충돌 고지라 남긴다.
- ⚠️ **본문은 플레인 텍스트다**(2026-09-09). 리치텍스트는 HTML 저장 → 새니타이저가
  필요해지고, 판정 대상이 AI가 만든 페이지라 복붙하면 저쪽 마크업이 통째로 들어온다.
  자동 링크는 `core/text.ts`가 쪼개고 `Body.svelte`가 그린다 — `{@html}` 금지.
  플레인 → 마크다운은 나중에 올라갈 수 있지만 리치텍스트에서는 못 내려온다.
- ⚠️ **글은 바로 게시된다**(2026-09-09 유저 지시 — "긱뉴스처럼"). 사전 검토 게이트를
  되살리지 마라. 그 자리를 메우는 건 사후 창구 셋이다 — 글 신고(`post_report`, 비로그인) ·
  정정 요청 · 당사자 답변. 하나라도 없애면 실명 판정 사이트에 방어선이 없다.
  운영자 대응은 `hidePost`(soft delete + 감사 로그) 하나다.
- **삭제 대신 soft delete + 감사 로그.** `audit_log`에 DELETE 경로를 만들지 마라 —
  D1엔 권한으로 막을 방법이 없어 "코드에 그 경로가 없다"가 유일한 강제다.
- **돈 받고 지우는 기능 금지.** 결제·삭제요청을 잇는 어떤 배선도 만들지 마라.
- **당사자 답변·정정은 비로그인 제출**(가입을 요구하면 반론 창구가 닫힌다). 정정은
  수용·반려 **둘 다** `/about#정정`에 공개한다. ⚠️ 둘의 자리는 상세가 아니라
  `/posts/[slug]/reply`다(2026-09-09 유저 지시) — 상세에 접어 두면 읽으러 온 사람에겐
  잡음이고 정작 쓸 업체는 끝까지 안 내려온다. 상세엔 `신고`와 그 페이지 링크만 있다.
- ⚠️ **시드에 실제 판정을 넣지 마라.** 저장소가 공개라 판정 데이터가 들어가면 사이트가
  통째로 복제 가능해진다. 로컬 시연 데이터는 `seed.local.sql`뿐이고(`is_demo = 1`),
  규칙은 그 파일 머리에 있다. ⚠️ `--remote`로 돌리지 마라.

## 디자인 — Lobste.rs 이식 (2026-09-08 유저 선택)

**핵심은 AI 냄새를 빼는 것.** 그림자·그라데이션·트랜지션·콜아웃(좌측 액센트 바 + 틴트
배경)·이모지·웹폰트 금지 — 지금 AI가 뱉는 화면의 문법이다. 차단 블록이 `app.css` 맨
아래에 있다(no transition/animation/box-shadow). 지우면 디자인이 조용히 되돌아간다.

- ⚠️ **색을 지어내지 마라**(유저 지시). 출처는 셋뿐 — lobste.rs `light-normal` 실측값,
  Tailwind 기본 팔레트, ProductHunt 실측값. `app.css`에 출처를 적어 뒀다.
- ⚠️ **색이 있는 자리는 제호뿐이다**(2026-09-08 유저 지시 — "포인트색없이 로고만있게").
  구분은 색이 아니라 **명도와 무게**로: AI 여부 = 테두리만, 문제 있음 = 검정 채움,
  괜찮음 = 테두리 진하게, 눌린 표 = 진하게. 링크도 무채색이다(`--color-link: #101828`,
  lobste.rs 파랑에서 뺐다). 산문 링크는 `.prose a` **밑줄**로 알린다.
  ⚠️ 예외는 둘(2026-09-09 유저 지시 — "리스트에 뱃지처럼 갈색으로") — 목록 여론
  네모(`.opinion`)와 상세 `.btn-slop`이 같은 `--color-negative`를 쓴다.
  **"괜찮다"에는 칠하지 마라** — 둘 다 칠하면 신호등이 되고 색이 어느 쪽을 누르라고 민다.
- **브랜드색은 똥색**(`--color-sludge: #854d0e` = Tailwind yellow-800), 제호·favicon에만.
  바꿔야 하면 Tailwind yellow/amber 800~900에서 골라라. ⚠️ 제호에 배경을 깔지 마라 —
  색 블록이면 판정 칩과 같은 덩어리로 보인다. 제호는 글자, 판정은 칩.
- **종이는 흰색**(2026-09-08 유저 지시 — "쓰레기 사이트가 되어야 한다. 근데 배경색은
  흰색이다. 랍스터처럼"). 판정 기관이 똥색이면 판정을 못 믿는다.
- **제호는 도메인 그대로 `aislop.kr`**(FactCheck.org 방식) + 똥 실루엣 마크.
  스크린샷으로 퍼져도 출처가 같이 간다. `.kr`은 브랜드색 밖. ⚠️ 같은 도형이
  `static/favicon.svg`와 `+layout.svelte`에 각각 인라인이다 — 한쪽만 고치지 마라.
  이모지(💩)로 대체 금지(폰트마다 다르게 그려지고 이 사이트는 이모지를 안 쓴다).
- **구조**(전부 원본 실측): wrapper `76rem` · 산문 `.prose` `45rem` · 좌측 40px 투표 칼럼 ·
  제목 + (도메인) + 칩 한 줄 · byline 한 줄. **목록은 넓게 훑고 본문은 좁게 읽는다** —
  컨테이너 폭과 산문 폭을 같게 만들지 마라.
- ⚠️ **타이포 바닥은 14px다**(2026-09-08 유저 지시 — "프러덕트헌트처럼 큼지막하게").
  페이지 제목 24 · 목록 행 제목 20 · 소제목 18 · 본문 16 · 메타·칩 14.
  lobste.rs 원본(.meta 0.8rem, .tag 0.66rem)으로 되돌리지 마라 — 라틴 기준이라
  한글은 같은 크기에서 훨씬 흐려진다.
- ⚠️ **byline 링크는 회색**(`.meta a { color: inherit }`) — 아이디·댓글·도메인까지
  파래지면 한 행에 파란 덩어리가 넷이 되어 제목이 안 보인다.
  ⚠️ `:not(.btn)`을 유지해라. 안 그러면 `.btn-primary` 흰 글씨가 회색으로 덮인다.
- ⚠️ **피드 한 행 = 두 줄, 칩 하나**(2026-09-08 유저 지적 — "글이 너무 많아 피로").
  여론이 있으면 여론 칩, 없으면 없다. AI 상태·분류는 byline 평문. 요약문을 피드에
  되살리지 마라. 정보량 없는 값(미판정·AI 불명·AI 정황)은 목록에서 아예 뺀다
  (`AI_STATUS_IN_FEED`). 상세(`full`)에서는 두 축을 전부 보여준다.
  **밀도는 폭이 아니라 행당 정보량 문제다** — 컨테이너를 넓히면 줄만 길어진다.
- **헤더 탭은 랍스타 구조**: 활발 / 최신 / 댓글 / 검색. 여론별 목록과 `AI 확인`은 탭이
  아니라 필터 줄(`OPINION_FILTERS`)이고 라벨은 한글이다. ⚠️ 필터를 운영자 축
  (검증됨·논쟁 중·Not Slop)으로 되돌리지 마라 — 운영자 판정이 없어서 목록이 통째로 빈다.
- **제목은 우리 기록으로, 도메인은 원문으로.** 제목을 원문에 걸지 마라 — 감별 미디어가
  감별 대상에게 트래픽을 보내는 구조가 된다. 원문은 괄호 안 도메인에
  `rel="nofollow ugc noopener"`로만. 슬롭은 지적당하면 잘 내려가고(그래서 `archiveUrl`),
  목격담은 원문이 아예 없기도 하다 — 제목이 원문이면 깨진 링크가 대문이 된다.
- **상세는 평범한 글이다**(유저 지적 — "너무 체계있어서 피곤"). 상단은 피드 행과 같은 줄.
  네 질문은 스키마에만 남기고 화면에서 소제목으로 칸을 나누지 마라. 시각적으로 떼는
  블록은 **당사자 답변**뿐이다.
- 재사용 클래스: `.score .tag .tag-media .tag-meta .tag-negative .tag-affirmative .box
  .bar .btn .btn-primary .meta .num .mark`
- **거쳐 온 것**(다시 제안하지 마라): GeekNews 모방("너무 똑같다") · 2000년대 한국
  게시판("구시대 UI도 보기 힘들다") · 배경 전체 똥색 · 판정색 원색 빨강/초록(렉카 배지) ·
  똥색·이끼색 판정칩 · `ais` 모노그램 · 헤더 한글 병기 · 태그라인.

## 문장 — AI 말투 배제

⚠️ 취향이 아니라 **기능**이다. AI 슬롭을 판정하는 사이트가 AI가 쓴 것처럼 읽히면 그
자체가 반박 재료가 된다. 디자인의 AI 냄새 차단 블록과 같은 급이다.

- **문체는 자리로 나눈다.** 산문은 평서형 `~다`(`/about`·`/ai-slop`·원칙·정정 정책) ·
  표·목록·힌트는 명사형(`taxonomy.ts` 힌트, `dd`, 분류 설명) · UI 부스러기는 음슴체
  (캡션·라벨·버튼·토스트·에러·빈 화면).
  ⚠️ `~습니다`로 되돌리지 마라(2026-09-09 유저 지시 — "니다 반복이 ㅈㄴ 불쾌하다.
  AI로 만든 사이트나 광고도 다 이런 식"). `~다`는 반말이 아니라 신문·백서 문체라
  고발 미디어에 맞다. 해체(`~해`·`~야`)로 내려가는 건 하지 마라.
  ⚠️ **음슴체는 `~함/~음`이지 `~해라`가 아니다**(2026-09-08 유저 지적 — "너무 예의없게는
  하지 마라"): `신고 사유를 적어라`(X) → `신고 사유를 입력`(O). 기준선은
  `login/+page.server.ts`. ⚠️ 에러 문체를 두 갈래로 만들지 마라(2026-09-08 통일).
- ⚠️ **나열을 셋으로 맞추지 마라**(2026-09-09). `A하고, B하고, C합니다`가 겹치면 그 리듬
  자체가 AI의 서명이다. 사람은 둘로 끝내거나 넷까지 늘어놓거나 하나만 던진다.
- **문장 길이를 들쭉날쭉하게.** 조각 문장 허용. 같은 길이가 다섯 개 이어지면 AI 티가 난다.
- **굵게는 문단당 하나.** 문단을 `그래서 …입니다`로 닫지 마라.
- **청유·부탁형 금지.** `적어 주십시오` → `적으면 된다`. 무료 사이트가 굽신거릴 이유가 없다.
- ⚠️ **문장 끝을 em dash로 받지 마라**(산문 한정. 이 CLAUDE.md는 개발자용이라 써도 된다).
- **반복 템플릿 금지.** 여러 화면이 `**굵게** — 설명.` 모양으로 늘어서면 그 규칙성이 곧
  AI 티다. 탭 캡션 넷·목록 캡션 넷을 2026-09-08에 걷어냈다(목록 이름이 곧 설명이라
  정보량 0). 두 축 혼동을 막는 `/verified` 한 줄만 남았다 — 나머지에 다시 달지 마라.
- **따옴표 뜻풀이 금지.** 뜻을 설명해야 한다면 문장이 잘못된 것이다.
- **피동·명사화를 능동으로**(접수되었습니다 → 접수했습니다). **라벨은 명사, 버튼은 동사**
  (`어떤 유형인가요?` → `유형`).
- **사과·격려·감탄·이모지·느낌표 금지.** 빈 화면에 "첫 제보를 기다립니다" 류 금지
  (유저 지시). 에러는 무엇이 틀렸고 뭘 하면 되는지만.
- **같은 행동은 같은 단어로.** 사례를 올리는 행동은 **글쓰기**다(2026-09-09 유저 지시) —
  스코프가 넓어져서 `제보`는 고발만 받는 것처럼 읽힌다. `제보하기`로 되돌리지 마라.
- 판정 문구는 단정형 대신 **관찰 + 근거**로. 명예훼손 방어선(형법 310조)이 여기 걸린다.

## 구조 결정

- 닫힌 도메인의 진실원은 `core/taxonomy.ts`의 TS union. DB엔 CHECK로만 복제하고 룩업
  테이블을 만들지 마라. 화면 라벨도 여기서만 고친다.
- **DB에 닿는 모듈에서 화면용 상수를 export하지 마라.** `FEED_TABS`가 `feed.ts`에 있었을 때
  레이아웃이 값으로 import해 `node:async_hooks`가 클라이언트 번들로 끌려갔다 —
  SSR은 200인데 하이드레이션만 죽어서 curl로는 안 잡힌다.
- **분류 숨기기(`/filters`) 화면을 되살리지 마라**(2026-09-08 유저 지시 — "헤더에 이게
  다 있네"). `CategoryBar`가 이미 하는 일이라 쿠키·서버액션·라우트만 늘었다.
- 시각은 ISO8601 KST 문자열(`core/time.ts`). 예외 없다.
- 권한 검사는 서버에서만. `locals.user`는 `hooks.server.ts`가 유일하게 채운다.
- 평판은 계산값이다. 캐시 테이블을 만들지 마라. 배지는 손으로 붙인다 — 자동 부여를
  만들면 제보 수 늘리기 게임이 시작된다.

## 함정

- **락파일이 둘이다.** 로컬 bun(`bun.lock`, gitignore), CI npm(`package-lock.json`, 커밋).
  ⚠️ CF Workers Builds의 bun 1.2.15가 lockfileVersion 2를 못 읽는다. 의존성을 바꾸면
  **둘 다**: `bun install && npm install --package-lock-only`.
- **`.kr` 도메인**: CF 레지스트라가 `.kr`을 취급 안 한다. 국내 등록기관 등록 + 네임서버만
  CF 위임 상태여야 Worker custom domain이 붙는다.
- **한글 slug + Location 헤더**: non-ASCII를 못 싣는다. `redirect()`엔 반드시
  `encodeURIComponent`. `params.slug`는 이미 디코딩된 값이다.
- **zod `z.url()`은 스킴을 안 본다** — `javascript:alert(1)`이 통과해 XSS가 된다.
  `validate.ts`의 `httpUrl()` / `core/url.ts`의 `safeHttpUrl()`을 써라. 관리자 입력도.
- **로그인 = 이메일 OTP 하나뿐**(2026-09-08). 비밀번호도 소셜도 없다 — 콘솔 등록 3곳·
  시크릿 4개가 배포를 막고 있었고, 카카오는 이메일이 잠긴 항목이라 자리표시자를 심어야 했다.
- **인증 라이브러리를 쓰지 않는다**(better-auth 제거, 2026-09-08 유저 지시). 전부
  `core/auth.ts` 한 파일, 의존성 없음. 규칙 넷은 `auth.test.ts`가 묶는다: `getRandomValues`만 ·
  해시로만 저장 · 상수 시간 비교 · 5분 3회. ⚠️ 다시 넣지 마라 — 쓰던 기능이 5%였다.
- ⚠️ **회원 식별자는 이메일과 닉네임(`name`) 둘뿐이다**(2026-09-09 유저 지시 — "걍 이메일,
  닉네임 두 개로 가면 안 되냐"). `username`을 걷어냈다(`drizzle/0010`). `name`은
  UNIQUE(사칭 차단)고 프로필 주소는 `/users/<id>`라 이름을 바꿔도 링크가 안 죽는다.
  옛 주소 리다이렉트 표를 만들지 마라 — "예전 이름"이 영구 기록으로 남는다.
  이메일 앞부분을 이름으로 쓰지 마라(작성자가 공개되므로 이메일이 공개된다).
  OTP는 이름을 안 줘서 `ensureName`이 `slop-xxxx`를 발급한다.
- **dev는 늘 로그인 상태다**(`.dev.vars`의 `DEV_USER_ID`). ⚠️ **화면을 고칠 땐 로그아웃
  상태를 따로 봐라** — 이걸 안 해서 비로그인 방문자에게 기여 동선이 하나도 없는 걸 오래
  못 봤다(2026-09-10). `DEV_USER_ID`를 주석 처리하고 `bun run dev --port 3301`
  (값은 부팅 때 한 번만 읽으니 띄운 뒤 되돌리면 된다).
- **빈 dev DB에서는 판정 화면을 못 본다**(표가 없으면 전부 "표 없음").
  `bun run db:seed:local` — 여섯 상태를 하나씩 깐다. 그 균형을 깨지 마라.
- **drizzle 에러**: UNIQUE 위반 메시지는 `cause` 체인에 있다 — `isUniqueViolation()`.
- **CSP**: 외부 출처를 붙일 땐 `hooks.server.ts`부터. 안 고치면 조용히 차단된다
  (썸네일이 외부 도메인이면 `img-src`에 추가 — 지금은 cdn.aislop.kr만).

## 판정 문서

- ⚠️ **`/ai-slop`의 구별법은 유통기한이 있다.** "손가락 여섯 개·뭉개진 글자"는 2023~24년
  모델 흔적이라 2026-09-08에 걷어냈다. **판정 기관이 낡은 판별법을 가르치면 그 자체가
  반박 재료가 된다.** 지금 순서는 계정 → 출처 → 여러 장 사이의 일관성 → 글 → 이미지
  자체(보조)다. **주변 신호를 위에 두는 순서를 뒤집지 마라.**
- ⚠️ **`/about` 첫 두 문단(피로) → `#왜`(공공의 이익) 순서를 뒤집지 마라**(2026-09-08
  유저 지시). 사람을 부르는 건 "지겹다"는 공감이고 판정을 지탱하는 건 공공의 이익이다.
  감정이 판정 근거 자리로 올라가면 "AI 싫어하는 애들"이 되고, 동기를 빼면 소비자원
  안내문이 된다. `/about`에 "역겹다" 류 금지 — 기업 법무가 읽는 문서다. 그 감정은
  홈과 사례 본문에서 쓴다.
- ⚠️ **`/about#왜`와 `#운영`을 지우지 마라.** 실명 서비스를 판정하므로 형법 310조
  (진실한 사실 + 공공의 이익)가 방어선이고, 목적을 사이트가 스스로 밝혀 두는 게 출발점이다.
  운영 주체·연락처(ekankr2@gmail.com)·후원 없음·광고 없음도 같은 이유로 공개한다.
- ⚠️ **푸터에 `by eklab` 크레딧·링크를 달지 마라**(2026-09-08). `#운영`의 "광고와 제휴
  링크를 싣지 않습니다"와 부딪히고, 판정당한 쪽이 "웹 개발 영리 업체가 운영한다" 한 줄로
  판정 전체를 홍보로 재프레이밍한다. eklab은 `#운영`에 **링크 없이 이해충돌 고지로만**
  적혀 있다. 홍보가 필요하면 방향을 뒤집어라 — eklab 쪽에서 제작 사례로 소개한다.

## SEO

**"AI 슬롭"·"AI slop"은 한국에서 빈 키워드다**(2026-09-08). 빈 건 경쟁이지 수요가 아니다 —
검색량이 0이라 그 키워드만으로는 유입이 안 온다.

- **이름 = 브랜드용, 유입 = 다른 키워드.** 실제 질의는 "AI 그림 구별법", "챗지피티 글 티",
  "AI 소설 전자책", "가짜 후기 구별" 쪽이다. 그 질의에 답하는 글이 유입을 만들고 거기서
  "AI 슬롭"을 반복 노출해 브랜드 키워드를 나중에 키운다. **순서를 뒤집지 마라.**
- 제목 접미사는 `AI 슬롭(AI slop)`, 전 페이지 공통(`Seo.svelte`). ⚠️ 가운뎃점으로
  되돌리지 마라 — 홈에서 구분자가 겹치고 두 표기가 별개 이름처럼 읽힌다.
- ⚠️ **홈 title은 키워드를 노리지 않는다**(2026-09-08 유저 결정). "AI 슬롭 뜻"류는
  `/ai-slop`이 먹는 질의고, 홈이 같이 밀면 서로 깎는다. 홈 title의 일은 공유됐을 때 뭐
  하는 곳인지 한 줄로 알리는 것 하나다. 지금 문장이 하는 일 셋 — 스코프(전부) ·
  커뮤니티("같이") · 판단 주체가 유저. **셋 중 하나도 빼지 마라.** 같은 문장을
  rss.xml·auth.ts 메일 서명도 쓴다.
  거쳐 온 것: "근거로 판정합니다"(커뮤니티 신호 0) · "지친 사람들이 사례를 모읍니다"
  (슬롭만 모으는 곳으로 읽힘) · "기록합니다"(위키) · "지겨운 AI"(사이트 전제와 충돌).
- `/ai-slop` = 용어 정의 페이지. **유일하게 SEO가 존재 이유인 문서**이고 언론이 인용할 때
  걸리는 앵커다. `/about`(원칙·정정)과 합치지 마라. ⚠️ `/about`에 정의를 다시 풀어
  쓰지 마라 — 같은 예시 문장을 쓰면 구글이 유사문서로 보고 서로 깎는다(2026-09-08 분리).
- JSON-LD: 홈에 `WebSite`+`Organization`(`siteJsonLd`, `alternateName`이 두 표기를 한
  엔티티로 묶는다), `/ai-slop`에 `Article`+`DefinedTerm`(날짜는 손으로).
  ⚠️ **ClaimReview를 내보내지 마라** — 운영자가 확정하는 사실 필드가 없어 붙일 자리가
  없고, 유저 표는 의견이라 붙이면 구글 수동 조치 대상이다. 고정은 `jsonld.test.ts`.
- 문서 페이지는 `<h1>`으로 시작하고 절마다 `id`를 단다(언론이 딥링크한다).

## 상태

- MVP 동작: 피드(탭 4 + 여론·분류 필터) · 상세 · 글쓰기(즉시 게시) · 이메일 OTP 로그인 ·
  투표 · 댓글 · 신고 · 관리자 사실 확인 · 당사자 답변 · 정정 · 검색 · 프로필 · sitemap/RSS.
- **2026-09-08 배포.** aislop.kr 실서비스, www→apex 301, 원격 D1 마이그레이션 적용.
  시크릿은 없다. 메일 SPF·DMARC(`p=reject`) 적용, **지메일 인박스 도달 확인(2026-09-10)**.
- 세션 30일 고정(슬라이딩 아님), 코드 5분·3회.
- 메일은 셋뿐이다 — 로그인 코드 · 창구 접수 알림(`ADMIN_EMAIL`) · 처리 결과(반려도 보낸다).
  셋 다 상대가 먼저 행동해서 생기는 1:1 메일이다. ⚠️ 구독·마케팅을 붙이지 마라 —
  스팸 신고를 맞으면 로그인 메일까지 죽는다. 알림은 `notify()`라 실패를 삼키고
  (접수는 이미 끝났다), 로그인 코드는 그대로 던진다.
- **남은 것**: 사례 3건(2026-09-10) — 병목은 여전히 물량이다. 사례별 OG 이미지(공용 1장).
